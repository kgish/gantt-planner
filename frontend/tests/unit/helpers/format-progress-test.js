
import { formatProgress } from 'frontend/helpers/format-progress';
import { module, test } from 'qunit';

module('Unit | Helper | format progress');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = formatProgress([42]);
  assert.ok(result);
});

