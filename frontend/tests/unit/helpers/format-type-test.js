
import { formatType } from 'frontend/helpers/format-type';
import { module, test } from 'qunit';

module('Unit | Helper | format type');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = formatType([42]);
  assert.ok(result);
});

